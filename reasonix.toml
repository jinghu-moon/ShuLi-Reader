# Reasonix configuration.
# Resolution order: flag > ./reasonix.toml > ~/.config/reasonix/config.toml > built-in defaults.
# Secrets come from the environment via api_key_env; never put keys here.

config_version = 2   # schema marker for diagnostics; old versions may ignore it
default_model = "deepseek-flash"
# language      = "zh"   # ui/model language; empty = auto-detect from $LANG / $REASONIX_LANG

[agent]
# system_prompt = """..."""   # omit to use the built-in prompt for this version
# system_prompt_file = "prompts/system.md"   # overrides system_prompt when set
max_steps         = 0   # executor tool-call rounds; 0 = no limit
planner_max_steps = 12   # planner read-only tool-call rounds; 0 = no limit
temperature       = 0.0
auto_plan   = "off"   # off|on; off keeps plan mode manual
# auto_plan_classifier = "deepseek-flash"   # optional; only used for borderline tasks
soft_compact_ratio  = 0.5   # notice only; keeps cache-first prefix intact
compact_ratio       = 0.8   # try compacting when prompt reaches this fraction
compact_force_ratio = 0.9   # force compacting at this high-water mark
# planner_model = "mimo"   # optional: enable two-model collaboration
# subagent_model = "deepseek-pro"   # optional default for runAs=subagent skills
# subagent_models = { review = "deepseek-pro", security_review = "deepseek-pro" }   # per-skill overrides
# subagent_effort = "high"   # optional default effort for subagents
# subagent_efforts = { review = "max", task = "high" }   # per-tool/skill effort overrides
# output_style = "explanatory"   # explanatory | learning | concise | custom; empty = default

[tools]
enabled = []   # empty = all built-in tools
bash_timeout_seconds = 120   # foreground safety cap; set 0 for no tool-local cap

[codegraph]
enabled      = true   # built-in MCP server; off by default for first-run sessions
auto_install = true   # fetch the runtime when CodeGraph is enabled but missing
# path       = ""   # empty = cache, then PATH, then a bundle beside reasonix

[lsp]
enabled = true   # language server tools; servers launch lazily when used
# [lsp.servers.go]
# command = "gopls"
# args = []
# extensions = [".go"]

[skills]
# paths = ["~/my-skills", "../shared/skills"]   # extra custom skill roots
# excluded_paths = ["~/.agents/skills"]   # hide convention roots without deleting folders
# max_depth = 3   # nested scan depth; set 1 for legacy root-only discovery
# disabled_skills = ["review"]   # hide noisy or unwanted skills

[permissions]
# Per-call gating. mode = writer fallback when no rule matches: ask|allow|deny.
# Readers always default to allow. Precedence: deny > ask > allow > fallback.
# Rules are "Tool" or "Tool(specifier)"; e.g. Bash(go test:*), Edit(src/**).
mode  = "ask"
# deny = ["Bash(rm -rf*)", "Bash(git push*)"]   # hard-blocked in every mode
allow = ["task", "Bash(cd D:\\100_Projects\\110_Daily\\ShuLi-Reader ; git status)", "Bash(cd D:\\100_Projects\\110_Daily\\ShuLi-Reader ; git diff --stat)", "Bash(cd D:\\100_Projects\\110_Daily\\ShuLi-Reader ; git diff --stat --cached)", "Bash(cd D:\\100_Projects\\110_Daily\\ShuLi-Reader ; git diff app/src/main/java/com/shuli/reader/feature/reader/render/ReaderCanvasStateApplier.kt)", "Bash(cd D:\\100_Projects\\110_Daily\\ShuLi-Reader ; git diff app/src/main/java/com/shuli/reader/core/recorder/CanvasRecorderLocked.kt)", "Bash(cd D:\\100_Projects\\110_Daily\\ShuLi-Reader ; git diff app/src/main/java/com/shuli/reader/core/reader/model/TextModels.kt)", "Bash(cd D:\\100_Projects\\110_Daily\\ShuLi-Reader ; git diff app/src/main/java/com/shuli/reader/feature/reader/session/ReaderNavigationCoordinator.kt)", "Bash(cd D:\\100_Projects\\110_Daily\\ShuLi-Reader ; git diff app/src/main/java/com/shuli/reader/core/reader/engine/ReaderCanvasView.kt | head -200)", "Bash(cd D:\\100_Projects\\110_Daily\\ShuLi-Reader ; git diff -- app/src/main/java/com/shuli/reader/core/reader/engine/ReaderCanvasView.kt | Select-Object -First 200)", "Bash(cd D:\\100_Projects\\110_Daily\\ShuLi-Reader ; git diff -- app/src/main/java/com/shuli/reader/core/reader/engine/cache/PageBitmapCache.kt | Select-Object -First 150)", "Bash(cd D:\\100_Projects\\110_Daily\\ShuLi-Reader ; git diff -- app/src/main/java/com/shuli/reader/core/reader/engine/CanvasVisualParamsManager.kt | Select-Object -First 40)", "Bash(cd D:\\100_Projects\\110_Daily\\ShuLi-Reader ; git diff -- app/src/main/java/com/shuli/reader/core/reader/engine/ReaderPageRenderer.kt | Select-Object -First 80)", "Bash(cd D:\\100_Projects\\110_Daily\\ShuLi-Reader ; git diff -- app/src/main/java/com/shuli/reader/feature/reader/session/ChapterPaginationCoordinator.kt | Select-Object -First 80)", "Bash(cd D:\\100_Projects\\110_Daily\\ShuLi-Reader ; git diff -- app/src/main/java/com/shuli/reader/feature/reader/render/ReaderRenderSnapshot.kt)", "Bash(cd D:\\100_Projects\\110_Daily\\ShuLi-Reader ; git diff -- app/src/main/java/com/shuli/reader/feature/reader/render/ReaderRenderSnapshotFactory.kt)", "Bash(cd D:\\100_Projects\\110_Daily\\ShuLi-Reader ; git diff -- app/src/main/java/com/shuli/reader/feature/reader/render/InvalidationScope.kt)", "Bash(cd D:\\100_Projects\\110_Daily\\ShuLi-Reader ; git add -A)", "Bash(cd D:\\100_Projects\\110_Daily\\ShuLi-Reader ; git commit -m @'\nrefactor(reader): Phase 1+2a 渲染架构根治 — 止血 + 模型纯化\n\n按照 docs/34-reader-render-architecture-refactor.md 实施:\n\nPhase 1 — 止血:\n  1.1 删除 RenderNodePool，每实例自持 RenderNode\n  1.2 CanvasRecorderLocked: recycled 路径结束 dummyPicture 录制\n  1.4 onDraw() idle 路径增加 overlayRecorder.draw()\n  1.5 StateFlow: 全线改用 .update {} 原子操作替代 .value = copy\n      (ReaderNavigationCoordinator + ChapterPaginationCoordinator)\n  1.6 reflow 增加 150ms debounce\n  1.7 ReaderCanvasStateApplier: 先收集所有 invalidate，最后统一 submitRenderTask\n  1.8 CanvasVisualParamsManager: 移除 onSubmitRenderTask 回调，只做参数应用\n\nPhase 2a — 模型纯化:\n  - TextPage/TextLine 不再持有 CanvasRecorder，改为纯分页模型\n  - 创建 PageRenderStateStore 统一管理 recorder 生命周期\n  - 创建 PageRenderState 封装单页的 shell/content/overlay/composite recorder\n  - 行级 recorder 由 store 按 LineKey 索引管理\n  - 创建 PaintSnapshot / RenderContextSnapshot / StatelessReaderPageRenderer\n    (Phase 3 基础设施，后台只读不可变快照)\n  - PageBitmapCache.doRecordPage 接收 renderState 参数而非从 page 读取\n  - chapterContentsByIndex 改为不可变 Map 快照\n\nPhase 5 — key-diff 驱动失效（初期）:\n  - InvalidationScope 标记 @Deprecated，key-diff 替代 CONTENT/SHELL/OVERLAY scope\n  - applyKeyDiff 方法，比较 contentKey/shellKey/overlayKey/layoutKey\n  - ShellSnapshot 增加 colorTemperature 字段\n\n其他:\n  - ReaderCanvasView: 移除 setBatteryLevel/setColorTemperature 绕过路径\n  - 删除 TextPageTest（模型不再持有 recorder）\n'@)", "Bash(cd D:\\100_Projects\\110_Daily\\ShuLi-Reader ; git push)"]
# ask = ["Edit(src/**)"]   # force a prompt even if otherwise allowed

[sandbox]
# Confine tool blast radius. File-writers (write_file/edit_file/multi_edit)
# may only write under workspace_root (empty = current dir) + allow_write.
# bash = "enforce" (default) jails each command in an OS sandbox (macOS now;
# graceful fallback elsewhere); "off" disables it. network allows egress.
# workspace_root = ""            # default: current working directory
# allow_write = ["/tmp"]          # extra dirs writers may also modify
bash    = "enforce"
network = true

[statusline]
# A custom status line: a command whose first stdout line replaces the built-in
# data row. It receives {"model","contextUsed","contextWindow","cwd"} as JSON on stdin.
# command = "my-statusline.sh"

# External MCP servers. type: "stdio" (default, a subprocess) | "http" | "sse".
# ${VAR} / ${VAR:-default} are expanded from the environment in command/args/env/url/headers.
# [[plugins]]
# name    = "example"
# command = "reasonix-plugin-example"
# [[plugins]]                                  # a remote server over Streamable HTTP
# name    = "stripe"
# type    = "http"
# url     = "https://mcp.stripe.com"
# headers = { Authorization = "Bearer ${STRIPE_KEY}" }
